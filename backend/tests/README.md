# Backend Tests

Use this directory for route integration tests and cross-module workflow tests. Module-local service tests can live beside their modules when that keeps the feature easier to understand.
